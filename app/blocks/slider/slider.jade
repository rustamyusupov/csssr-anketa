mixin slider(data)
	each item in data
		+b.slider&attributes(attributes)
			+e.scale
				+e('span').pointer
			+e('ul').list&attributes(attributes)
				each level in item.levels
					+e('li').item
						| #{level}
