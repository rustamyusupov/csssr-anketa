mixin input-check(item, column)
	+b('label')(class='input-check_column_#{column}')
		+e('input').field&attributes({
			'id': item.attributes.name,
			'value': item.label,
			'type': 'checkbox'
		}, item.attributes)
		+e('span')(class='label_indent_#{item.attributes.name}') !{item.label}
